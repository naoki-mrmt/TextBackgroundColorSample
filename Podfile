platform :ios, '15.0'
use_frameworks!
install! 'cocoapods',
            :warn_for_unused_master_specs_repo => false

target 'TextBackgroundColorSample' do
  pod 'SwiftRichString'
  pod 'SwiftLint'
  pod 'LicensePlist'
end
